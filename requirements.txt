# PyTorch with CUDA 12.1 (Python 3.12 compatible)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.0+cpu
torchvision==0.19.0+cpu

# Diffusion models (updated for compatibility)
diffusers==0.30.0
transformers==4.44.0
accelerate==0.33.0
peft==0.12.0
safetensors==0.4.4

# Gradio & Spaces
gradio==4.42.0
spaces==0.30.0

# Utilities
pillow==10.4.0
numpy<2
invisible-watermark